[api]
# 本项目固定使用 DeepSeek 模型（deepseek/deepseek-chat），不可更换。
# 你只需要填两项：API 密钥与 base_url。
key      = "sk-e81919dd75ff4c7c88161485f82a76c9"
base_url = "https://dashscope.aliyuncs.com/compatible-mode/v1"   # 阿里云百炼 OpenAI 兼容接入点

[data]
repos_dir    = "./data/historical_repos"
metadata_dir = "./data/metadata"
cache_dir    = "./data/cache"   # 持久化符号索引 / FTS5 数据库（按仓库 hash 分文件）

[target]
# 可选：填写后 python agent.py 不带参数时使用此仓库；留空则要求命令行传入 --repo-id / --repo-path / --url
repo_id = ""

[engine]
rust_analyzer_timeout = 120   # 路径A 等待索引完成的最长秒数
clangd_timeout        = 60    # 路径B 等待索引完成的最长秒数
max_call_depth        = 3     # get_call_chain 默认展开层数
max_steps             = 30    # MCP server 每会话最大探索类工具调用步数（预算控制）。
                              # 子系统 prompt 要求探索调用 ≤12 次；30 为安全垫，超出即提示
                              # agent 停止探索、直接出报告。write_report/initialize_analysis
                              # 不计步、不受此限，故调低不会掐断写盘。原值 200 易让推理模型
                              # 在内存管理/设备驱动等大子系统里失控空转直至 600s 超时。
skip_dirs = ["vendor", "third_party", "target"]  # 路径C 跳过的目录
